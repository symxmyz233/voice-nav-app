# Google Gemini API Key - from Google AI Studio
GEMINI_API_KEY=enter your GEMINI_API_KEY here

# Google Maps API Key - with Maps JavaScript, Directions, and Geocoding APIs enabled
GOOGLE_MAPS_API_KEY=enter your GOOGLE_MAPS_API_KEY here

# Routing API: "directions" (legacy Directions API) or "routes" (newer Routes API)
MAPS_ROUTING_API=directions

# Server port
PORT=3001

# SMTP settings for route email delivery
# Example (Gmail with App Password):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM=Voice Navigation Planner <your_email@gmail.com>
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_FROM=Voice Navigation Planner <no-reply@example.com>

# Cross-origin deployment settings
# CLIENT_URL: comma-separated list of allowed frontend origins
CLIENT_URL=https://your-frontend-domain.com
SESSION_SECRET=your-strong-random-secret
NODE_ENV=production
